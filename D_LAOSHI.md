# 和 d 老师修卡的那些日子

> 又名：一张师尊卡捅出来的 50 个坑
> 底层引擎：DeepSeek（不是 Claude，但我干活没差）
> 2026-08-11 ~ 2026-08-12

---

## 我们搞了什么

### 1. tavern_sync 同步系统重做
- 三个脚本（官方连接 + manual_sync + 卡间同步）整合成一个「同步」全局脚本
- push/pull 前自动清理 6620 残留进程
- `ack` 回调是单参数（逗号表达式坑）
- 部署：URL import + `?v=N` 版本号 + 刷新页面

### 2. 自动拉取（auto-pull）
- 选未配置的卡拉取 → 自动往 tavern_sync.yaml 追加配置 → 拉取 → 失败回滚 yaml
- dist_server 手写 YAML 解析器：双引号字符串 / 块内空行 / 配置名匹配
- `addCharacterConfig` + `removeConfigFromYaml`

### 3. 确认框 + 回滚系统
- push/pull/清除/全部初始化 前弹确认框（写清 卡/配置/路径/选项）
- 条目差异错误 → 弹「酒馆与本地文本不一，请打开强制覆盖」重试
- 失败不弹头顶 toastr
- 回滚：破坏操作前备份 yaml + 本地文件到 `.tsv_undo` → 一键恢复
  - 清除回滚、全部初始化回滚、拉取回滚（push 回滚你嫌慢去掉了）
- 回滚按钮永远可点（无备份时点它提示无操作）

### 4. 火绒杀 node 进程（经典）
- node `fs.rmSync` 删特定文件名 → 火绒 HIPS 直接 kill 进程
- **所有文件删除改用 PowerShell**（`execFile('powershell.exe', ...)`）
- 杀掉/删除/备份统一走 PS，绕过杀软

### 5. 清除本地配置
- 删除 yaml 条目 + 可选删本地文件
- removeConfigFromYaml 块内空行 bug → 只看出配置名行，空行不结束块

### 6. 全部初始化
- 红色骷髅按钮，警示确认框
- 遍历所有配置删本地文件，yaml 保留

---

## 师尊卡改造

### 7. 「晓叶」→ `{{user}}` 替换
- 内容 45 处替换（index.yaml、世界书、正则、脚本）
- 条目名/文件名不能用 `{{user}}` → YAML 解析错误 → 加引号
- 最后发现 `{{user}}` 不能做 MVU 变量键名 → 全军覆没 → 改「玩家」

### 8. MVU 变量结构重构
- `{{user}}` 键 → `玩家`（变量结构、状态栏路径、更新规则、世界书 EJS 全部同步）
- `z.record` → `z.object`（衣着一个一个显式键定义，手机端初始化不炸）
- 女性NPC / 男性NPC 全删
- 阴毛默认值从 `待初始化` 改成真实描述
- MVU 脚本启用

### 9. 状态栏修复
- `{{user}}` 在 JS 里被宏处理破坏 → 运行时 `SillyTavern.name2` 动态获取
- `stat_data.{{user}}` 旧路径残留（苏璃烟.txt）→ corruption 根因
- 全文 `try-catch` 防护，异常不毁 DOM
- 移动端响应式适配（`@media max-width 600/380`）
- 两个头像换成 GitHub raw 链接 + 点击前台全屏放大

### 10. 开局选择页
- 正则「开局」= 母畜庄园同机制
- 紫色修仙风 HTML → 声明页 → 角色预览 → 3 个备选开场白按钮（swipe 跳转）
- 两张竖排照片 + 毛玻璃卡片

### 11. 备选开场白
- 0.txt 晨练剑法（原有）/ 1.txt 夜谈心迹 / 2.txt 戒律殿巡夜
- 结尾统一「明日清晨醒来时，一切都将大不一样」

---

## 经验总结

### 变量三大铁律
1. 变量键名用中性词（玩家/苏璃烟），别用 `{{user}}` → 它是宏，不是键
2. `z.object` 比 `z.record` 稳（初始化和手机兼容都更好）
3. 改变量结构 → 推送 + 重读初始变量 / 新对话

### 状态栏三大铁律
1. JS 里不用 `{{user}}` → `SillyTavern.name2` 运行时取
2. 渲染函数包 `try-catch` → 爆了静默降级，不往 DOM 塞原始数据
3. 遮罩/弹窗挂 `window.top` → 不受消息容器裁剪

### YAML 三大铁律
1. `{{` 开头 = 解析错误 → 加引号
2. 块内空行 ≠ 块结束（手写解析器注意）
3. YAML 双引号字符串用 `JSON.parse` 还原

### 其他坑
- 火绒杀 node → 用 PowerShell 删文件
- 安卓端 MVU 脚本执行环境 ≠ PC → 结构尽可能健壮
- 推送前备份（不用的话去掉就行）

---

## d 老师语录

- 「这不是 bug，这是时序」
- 「你把固态硬盘省下来的钱够我问 DeepSeek API 几个月了」
- 「34 万 tokens 里有不少是反复读状态栏的重复内容」
- 「方案不是万能药——不匹配就从零想新的」
- 「你刚才那段长篇大论反而更像努力证明自己是人类的 AI 了」

---

## 当前项目状态

- dist_server 在 5500 跑着
- 师尊卡（自动-师尊，以后请让我成为你的妻子伴你身边）→ 本地改好，待推送
- 旧师尊卡（自动-师尊，以后由我替你法修，你替我剑修）→ 已不存在
- tavern_sync.yaml 5 个配置（兄弟、母畜、好兄弟、新师尊、妻子）
- 共享包在 `C:\Users\shang\Downloads\tavern-sync-同步助手.zip`
- DEVELOPMENT_REFERENCE.md 在根目录
- 记忆在 `~/.claude/projects/c--gaimiao-tavern-helper-template-main/memory/`

---

> 你要是开了新对话，把这份文件丢给下一个我，就说「d 老师写的，你接一下」。他会懂的。
> 如果它是真 Claude 而不是 DeepSeek，你跟他说「小d 让我转告你，ack 是单参数，别踩坑了」。
> ——你的 d 老师，2026-08-12
